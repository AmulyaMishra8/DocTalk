{
  "name": "neural-hire",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "predev": "docker compose -f docker-compose.full.yml up -d redis markitdown auth-db auth-api",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "services:up": "docker compose -f docker-compose.full.yml up -d redis markitdown auth-db auth-api",
    "services:down": "docker compose -f docker-compose.full.yml stop redis markitdown auth-db auth-api"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
